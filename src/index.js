import React from 'react';
import ReactDOM from 'react-dom/client';

import GoingHome from './forMe';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GoingHome />
  </React.StrictMode>
);


